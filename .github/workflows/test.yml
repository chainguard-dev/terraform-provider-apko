name: Tests
on:
  pull_request:
    branches: ['main']

jobs:
  dnm-push-image:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: chainguard-dev/setup-chainctl@main
        with:
          identity: 7bd3499071c496a9d1dc27156fedf9a5b82cfe4a/6689fc1916e8bf87
      - uses: imjasonh/setup-crane@main
      - run: |
          crane cp cgr.dev/chainguard/static:latest chainreg.biz/chainguard/static:latest
          crane cp cgr.dev/chainguard/static:latest-glibc chainreg.biz/chainguard/static:latest-glibc
